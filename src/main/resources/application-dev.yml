spring:
  datasource:
    url: jdbc:mysql://localhost/via?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  application:
    name: via
logging:
  level:
    root: info
    com.hellozjf.via: debug